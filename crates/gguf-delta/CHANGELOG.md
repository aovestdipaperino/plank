# Changelog

## 0.1.0

- Initial release: `.ggd` format v1, `write_delta`, `read_header`,
  `read_chunks`, `apply`, `check_base`, `describe`, and the `gguf::layout`
  header reader. `testing` feature with an in-memory GGUF builder.
